type User{
  id: ID!
  email: String!
  password: String!
  records: [TwitterRecord!]!
}

type Query{
  users: [User]
  user(id: ID!): User
}

input UserInput {
  email: String!
  password: String!
}

type Mutation {
  createUser(input: UserInput!): User
}